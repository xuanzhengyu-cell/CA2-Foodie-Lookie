<nav class="navbar navbar-expand-sm bg-dark navbar-dark sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Foodie Lookie</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="collapsibleNavbar">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Explore Locations</a>
        </li>
        <% if (user != null) { %>
          <% for (let i = 0; i < location_ids.length; i++) {%>
            <li class="nav-item">
              <a class="nav-link" href="/location/<%= location_ids[i].location_id%>"><%= location_ids[i].location_name%></a>
            </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/profile">User Profile</a>
          </li>
          <% if (user.role === "admin") {%>
            <li class="nav-item">
              <a class="nav-link" href="/admin_dashboard">Admin Control</a>
            </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Sign Out</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/login">Sign In to Feast</a>
          </li>  
        <% } %>
      </ul>
    </div>
  </div>
</nav>
